# database assets

`migrations/0001_initial_schema.sql` is the initial schema for the MVP.

Tables:

- `users`
- `social_accounts`
- `scheduled_posts`
- `post_executions`

Apply this schema to Supabase Postgres before wiring production credentials.
